FROM --platform=linux/amd64 mariadb

# Get installation env vars
ARG DB_HOST
ARG DB_DATABASE
ARG DB_USERNAME
ARG DB_PASSWORD

ENV MYSQL_ROOT_HOST ${DB_HOST}
ENV MYSQL_DATABASE ${DB_DATABASE}
ENV MYSQL_USER ${DB_USERNAME}
ENV MYSQL_PASSWORD ${DB_PASSWORD}
ENV MARIADB_RANDOM_ROOT_PASSWORD true

